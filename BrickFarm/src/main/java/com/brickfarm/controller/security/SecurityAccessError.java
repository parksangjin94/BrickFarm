package com.brickfarm.controller.security;

//import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
//import org.springframework.security.core.Authentication;
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.GetMapping;

//@Controller // CommonController
//public class SecurityAccessError {
//	private static final Logger logger = LoggerFactory.getLogger(SecurityAccessDeniedHandler.class);

	/*
	 * @GetMapping("/accessError") public void accessDenied(Authentication auth,
	 * Model model) { model.addAttribute("msg", "접근의 권한이 없습니다."); }
	 * 
	 * 
	 * @GetMapping("/user/member/loginregistermodel") public void loginInput(String
	 * error, String logout, Model model) { logger.info("error : " + error);
	 * logger.info("Logout : " + logout);
	 * 
	 * if(error != null) { model.addAttribute("error",
	 * "login Error. Check Your Account"); } if(logout != null) {
	 * model.addAttribute("logout", "logout 되었습니다."); }
	 * 
	 * }
	 */

//}
